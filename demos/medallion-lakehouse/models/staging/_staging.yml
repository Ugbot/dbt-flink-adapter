version: 2

sources:
  - name: raw_data
    description: >
      Raw data sources generated by Flink's datagen connector.
      These provide synthetic event and user streams for the
      medallion pipeline demo.
    tables:
      - name: raw_events_source
        description: "Synthetic event stream (datagen)"
      - name: raw_users_source
        description: "Synthetic user data (datagen)"
